# Base URL of the AdCamouflage API as the browser sees it.
# NEXT_PUBLIC_* values are inlined at build time, so rebuild after changing this.
NEXT_PUBLIC_API_URL=http://localhost:8000

# Only needed when the backend runs with ADCAM_API_KEY set.
# NEXT_PUBLIC_API_KEY=
